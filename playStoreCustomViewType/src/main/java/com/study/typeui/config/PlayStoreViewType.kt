package com.study.typeui.config

enum class PlayStoreViewType {
// 특별히 생성자를 정의하지 않는 경우 위에서부터 순차적으로 0,1,2,3...의 값이 부여
//각 enum 인스턴스는 일반적으로 name과 ordinal이라는 프로퍼티를 가지며,name은 인스턴스의 이름을,ordinal은 인스턴스의 순서를 나타낸다.
    BANNER_LIST,
    HORIZONTAL_LIST,
    ICON_LIST
}